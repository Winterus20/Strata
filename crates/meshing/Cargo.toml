[package]
name = "strata-meshing"
version.workspace = true
edition.workspace = true

[dependencies]
strata-core = { path = "../core" }
glam.workspace = true
bytemuck = { version = "1", features = ["derive"] }
hashbrown.workspace = true
wgpu.workspace = true

[dev-dependencies]
criterion = "0.5"
strata-world-gen = { path = "../world-gen" }

[[bench]]
name = "meshing_bench"
harness = false